---
title: 'artifactType


  アーティファクトの種類'
menu:
  reference:
    identifier: ja-ref-query-panel-artifact-type
---

## Chainable Ops
<h3 id="artifactType-artifactVersions"><code>artifactType-artifactVersions</code></h3>

すべての[artifacts](artifact)の[artifactVersions](artifact-version)を[artifactType](artifact-type)から返します。

| 引数 |  |
| :--- | :--- |
| `artifactType` | [artifactType](artifact-type) |

#### Return Value
すべての[artifacts](artifact)の[artifactVersions](artifact-version)を[artifactType](artifact-type)から返します。

<h3 id="artifactType-artifacts"><code>artifactType-artifacts</code></h3>

[artifactType](artifact-type)の[artifacts](artifact)を返します。

| 引数 |  |
| :--- | :--- |
| `artifactType` | [artifactType](artifact-type) |

#### Return Value
[artifactType](artifact-type)の[artifacts](artifact)を返します。

<h3 id="artifactType-name"><code>artifactType-name</code></h3>

[artifactType](artifact-type)の名前を返します。

| 引数 |  |
| :--- | :--- |
| `artifactType` | [artifactType](artifact-type) |

#### Return Value
[artifactType](artifact-type)の名前を返します。

## List Ops
<h3 id="artifactType-artifactVersions"><code>artifactType-artifactVersions</code></h3>

すべての[artifacts](artifact)の[artifactVersions](artifact-version)を[artifactType](artifact-type)から返します。

| 引数 |  |
| :--- | :--- |
| `artifactType` | [artifactType](artifact-type) |

#### Return Value
すべての[artifacts](artifact)の[artifactVersions](artifact-version)を[artifactType](artifact-type)から返します。

<h3 id="artifactType-artifacts"><code>artifactType-artifacts</code></h3>

[artifactType](artifact-type)の[artifacts](artifact)を返します。

| 引数 |  |
| :--- | :--- |
| `artifactType` | [artifactType](artifact-type) |

#### Return Value
[artifactType](artifact-type)の[artifacts](artifact)を返します。

<h3 id="artifactType-name"><code>artifactType-name</code></h3>

[artifactType](artifact-type)の名前を返します。

| 引数 |  |
| :--- | :--- |
| `artifactType` | [artifactType](artifact-type) |

#### Return Value
[artifactType](artifact-type)の名前を返します。