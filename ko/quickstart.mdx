---
- title: "Quickstart: LLM 입력 및 출력 추적하기"
- description: 첫 번째 호출을 추적하려면 다음 단계를 따르세요
- mode: wide
---

<Tabs>
  <Tab title="Python">
    다음 Quickstart는 Deepnote에서 호스팅하는 내장 노트북을 사용하므로 로컬 머신에 환경을 설정하지 않고도 W\&B Weave를 사용해 볼 수 있습니다.

    API 키를 제공하고 노트북을 실행한 후, W\&B Weave 대시보드를 탐색하고 LLM 코드가 실행될 때 발생한 일을 관찰할 수 있는 실시간 URL을 얻게 됩니다.

    <iframe height="700" src="https://deepnote.com/app/weights-and-biases-10af/Weave-Quickstart-4a7f7fb1-5a3f-4961-9c36-aad0d2de2a0b?utm_source=app-settings&utm_medium=product-embed&utm_campaign=data-app&utm_content=4a7f7fb1-5a3f-4961-9c36-aad0d2de2a0b&__embedded=true" title="Weave Quickstart" width="800" allowfullscreen />

    API 키로 이 노트북을 실행한 후에는 Weave의 관찰 기능을 심층적으로 살펴볼 수 있는 실시간 Weave 대시보드가 제공됩니다.
  </Tab>

  <Tab title="TypeScript">
    ## Weave 설치 및 환경 설정

    먼저 필요한 라이브러리를 설치하세요:

    ```shell
    pnpm install weave dotenv
    ```

    그런 다음, Weights & Biases API 키를 다음에서 가져오세요: [https://wandb.ai/authorize](https://wandb.ai/authorize) 그리고 OpenAI API 키를 다음에서 가져오세요: [https://platform.openai.com/api-keys](https://platform.openai.com/api-keys).

    터미널에서 다음 명령을 실행하여 환경 변수를 설정하세요:

    ```bash
    export WANDB_API_KEY="your_wandb_key"
    export OPENAI_API_KEY="your_openai_key"
    ```

    또는 프로젝트 루트에 `.env` 파일을 생성할 수 있습니다:

    ```bash
    WANDB_API_KEY=your_wandb_key
    OPENAI_API_KEY=your_openai_key
    ```

    만약 `.env` 파일 접근 방식을 사용한다면, TypeScript 파일 상단에 dotenv를 가져오는 것을 잊지 마세요:

    ```typescript
    import 'dotenv/config';  // Add this at the top of your file
    ```

    ## LLM 호출 추적하기

    추적하려는 함수에 Weave 추적을 추가하세요:

    ```typescript
    // If using .env file, add this line:
    // import 'dotenv/config';
    import OpenAI from 'openai';
    // highlight-next-line
    import * as weave from 'weave';

    // highlight-next-line
    const openai = new OpenAI();

    async function extractDinos(input: string) {
      const response = await openai.chat.completions.create({
        model: 'gpt-4o',
        messages: [
          {
            role: 'user',
            content: `In JSON format extract a list of 'dinosaurs', with their 'name', their 'common_name', and whether its 'diet' is a herbivore or carnivore: ${input}`,
          },
        ],
      });
      return response.choices[0].message.content;
    }
    // highlight-next-line
    const extractDinosOp = weave.op(extractDinos);

    async function main() {
      // highlight-next-line
      await weave.init('examples');
      const result = await extractDinosOp(
        'I watched as a Tyrannosaurus rex (T. rex) chased after a Triceratops (Trike), both carnivore and herbivore locked in an ancient dance. Meanwhile, a gentle giant Brachiosaurus (Brachi) calmly munched on treetops, blissfully unaware of the chaos below.'
      );
      console.log(result);
    }

    main();

    ```

    당신이 `extractDinos` 함수를 호출하면 Weave는 추적을 볼 수 있는 링크를 출력합니다.
  </Tab>
</Tabs>

## 다음 단계

Weave로 함수를 계측하는 것은 시작에 불과합니다.

* 앱에서 [데이터 흐름 추적](./tutorial-tracing_2)하는 방법 알아보기
* 코드 성능 평가를 가능하게 하는 [Evaluation](/ko/tutorial-eval)에 대해 읽어보기
* 모든 AI 제공업체를 위한 [Integrations](./guides/integrations/index) 확인하기
* 다양한 모델을 테스트할 수 있는 [Playground](./guides/tools/playground) 사용해보기
