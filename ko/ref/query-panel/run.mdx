---
title: run
menu:
  reference:
    identifier: ko-ref-query-panel-run
---

## Chainable Ops
<h3 id="run-config"><code>run-config</code></h3>

[run](run)의 config _typedDict_를 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | [run](run) |

#### Return Value
[run](run)의 config _typedDict_

<h3 id="run-createdAt"><code>run-createdAt</code></h3>

[run](run)의 생성된 datetime을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 생성된 datetime

<h3 id="run-heartbeatAt"><code>run-heartbeatAt</code></h3>

[run](run)의 마지막 heartbeat datetime을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 마지막 heartbeat datetime

<h3 id="run-history"><code>run-history</code></h3>

[run](run)의 로그 history를 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 로그 history

<h3 id="run-jobType"><code>run-jobType</code></h3>

[run](run)의 job 유형을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 job 유형

<h3 id="run-loggedArtifactVersion"><code>run-loggedArtifactVersion</code></h3>

주어진 이름과 에일리어스에 대해 [run](run)에 의해 기록된 [artifactVersion](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |
| `artifactVersionName` | [artifactVersion](artifact-version)의 name:alias |

#### Return Value
주어진 이름과 에일리어스에 대해 [run](run)에 의해 기록된 [artifactVersion](artifact-version)

<h3 id="run-loggedArtifactVersions"><code>run-loggedArtifactVersions</code></h3>

[run](run)에 의해 기록된 모든 [artifactVersions](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)에 의해 기록된 [artifactVersions](artifact-version)

<h3 id="run-name"><code>run-name</code></h3>

[run](run)의 이름을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 이름

<h3 id="run-runtime"><code>run-runtime</code></h3>

[run](run)의 runtime(초)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 runtime(초)

<h3 id="run-summary"><code>run-summary</code></h3>

[run](run)의 summary _typedDict_를 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 summary _typedDict_

<h3 id="run-usedArtifactVersions"><code>run-usedArtifactVersions</code></h3>

[run](run)에서 사용한 모든 [artifactVersions](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)에서 사용한 [artifactVersions](artifact-version)

<h3 id="run-user"><code>run-user</code></h3>

[run](run)의 [user](user)를 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 [user](user)


## List Ops
<h3 id="run-config"><code>run-config</code></h3>

[run](run)의 config _typedDict_를 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 config _typedDict_

<h3 id="run-createdAt"><code>run-createdAt</code></h3>

[run](run)의 생성된 datetime을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 생성된 datetime

<h3 id="run-heartbeatAt"><code>run-heartbeatAt</code></h3>

[run](run)의 마지막 heartbeat datetime을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 마지막 heartbeat datetime

<h3 id="run-history"><code>run-history</code></h3>

[run](run)의 로그 history를 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 로그 history

<h3 id="run-jobType"><code>run-jobType</code></h3>

[run](run)의 job 유형을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 job 유형

<h3 id="run-loggedArtifactVersion"><code>run-loggedArtifactVersion</code></h3>

주어진 이름과 에일리어스에 대해 [run](run)에 의해 기록된 [artifactVersion](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |
| `artifactVersionName` | [artifactVersion](artifact-version)의 name:alias |

#### Return Value
주어진 이름과 에일리어스에 대해 [run](run)에 의해 기록된 [artifactVersion](artifact-version)

<h3 id="run-loggedArtifactVersions"><code>run-loggedArtifactVersions</code></h3>

[run](run)에 의해 기록된 모든 [artifactVersions](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)에 의해 기록된 [artifactVersions](artifact-version)

<h3 id="run-name"><code>run-name</code></h3>

[run](run)의 이름을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 이름

<h3 id="run-runtime"><code>run-runtime</code></h3>

[run](run)의 runtime(초)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 runtime(초)

<h3 id="run-summary"><code>run-summary</code></h3>

[run](run)의 summary _typedDict_를 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)의 summary _typedDict_

<h3 id="run-usedArtifactVersions"><code>run-usedArtifactVersions</code></h3>

[run](run)에서 사용한 모든 [artifactVersions](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `run` | A [run](run) |

#### Return Value
[run](run)에서 사용한 [artifactVersions](artifact-version)
