---
title: project
menu:
  reference:
    identifier: ko-ref-query-panel-project
---

## Chainable Ops
<h3 id="project-artifact"><code>project-artifact</code></h3>

[project](project) 내에서 지정된 이름의 [artifact](artifact)를 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |
| `artifactName` | [artifact](artifact)의 이름 |

#### Return Value
[project](project) 내에서 지정된 이름의 [artifact](artifact)

<h3 id="project-artifactType"><code>project-artifactType</code></h3>

[project](project) 내에서 지정된 이름의 [artifactType](artifact-type)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |
| `artifactType` | [artifactType](artifact-type)의 이름 |

#### Return Value
[project](project) 내에서 지정된 이름의 [artifactType](artifact-type)

<h3 id="project-artifactTypes"><code>project-artifactTypes</code></h3>

[project](project)에 대한 [artifactTypes](artifact-type)를 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)에 대한 [artifactTypes](artifact-type)

<h3 id="project-artifactVersion"><code>project-artifactVersion</code></h3>

[project](project) 내에서 지정된 이름 및 버전에 대한 [artifactVersion](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |
| `artifactName` | [artifactVersion](artifact-version)의 이름 |
| `artifactVersionAlias` | [artifactVersion](artifact-version)의 버전 에일리어스 |

#### Return Value
[project](project) 내에서 지정된 이름 및 버전에 대한 [artifactVersion](artifact-version)

<h3 id="project-createdAt"><code>project-createdAt</code></h3>

[project](project)의 생성 시간을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)의 생성 시간

<h3 id="project-name"><code>project-name</code></h3>

[project](project)의 이름을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)의 이름

<h3 id="project-runs"><code>project-runs</code></h3>

[project](project)에서 [runs](run)를 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)에서 [runs](run)

## List Ops
<h3 id="project-artifact"><code>project-artifact</code></h3>

[project](project) 내에서 지정된 이름의 [artifact](artifact)를 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |
| `artifactName` | [artifact](artifact)의 이름 |

#### Return Value
[project](project) 내에서 지정된 이름의 [artifact](artifact)

<h3 id="project-artifactType"><code>project-artifactType</code></h3>

[project](project) 내에서 지정된 이름의 [artifactType](artifact-type)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |
| `artifactType` | [artifactType](artifact-type)의 이름 |

#### Return Value
[project](project) 내에서 지정된 이름의 [artifactType](artifact-type)

<h3 id="project-artifactTypes"><code>project-artifactTypes</code></h3>

[project](project)에 대한 [artifactTypes](artifact-type)를 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)에 대한 [artifactTypes](artifact-type)

<h3 id="project-artifactVersion"><code>project-artifactVersion</code></h3>

[project](project) 내에서 지정된 이름 및 버전에 대한 [artifactVersion](artifact-version)을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |
| `artifactName` | [artifactVersion](artifact-version)의 이름 |
| `artifactVersionAlias` | [artifactVersion](artifact-version)의 버전 에일리어스 |

#### Return Value
[project](project) 내에서 지정된 이름 및 버전에 대한 [artifactVersion](artifact-version)

<h3 id="project-createdAt"><code>project-createdAt</code></h3>

[project](project)의 생성 시간을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)의 생성 시간

<h3 id="project-name"><code>project-name</code></h3>

[project](project)의 이름을 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)의 이름

<h3 id="project-runs"><code>project-runs</code></h3>

[project](project)에서 [runs](run)를 반환합니다.

| Argument |  |
| :--- | :--- |
| `project` | [project](project) |

#### Return Value
[project](project)에서 [runs](run)
