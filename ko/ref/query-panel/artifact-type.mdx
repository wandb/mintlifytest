---
title: artifactType
menu:
  reference:
    identifier: ko-ref-query-panel-artifact-type
---

## Chainable Ops
<h3 id="artifactType-artifactVersions"><code>artifactType-artifactVersions</code></h3>

[artifactType]( artifact-type) 의 모든 [artifacts]( artifact) 의 [artifactVersions]( artifact-version) 를 반환합니다.

| Argument |  |
| :--- | :--- |
| `artifactType` | [artifactType]( artifact-type) |

#### Return Value
[artifactType]( artifact-type) 의 모든 [artifacts]( artifact) 의 [artifactVersions](artifact-version)

<h3 id="artifactType-artifacts"><code>artifactType-artifacts</code></h3>

[artifactType](artifact-type) 의 [artifacts]( artifact) 를 반환합니다.

| Argument |  |
| :--- | :--- |
| `artifactType` | [artifactType](artifact-type) |

#### Return Value
[artifactType]( artifact-type) 의 [artifacts]( artifact)

<h3 id="artifactType-name"><code>artifactType-name</code></h3>

[artifactType](artifact-type) 의 이름을 반환합니다.

| Argument |  |
| :--- | :--- |
| `artifactType` | [artifactType]( artifact-type) |

#### Return Value
[artifactType]( artifact-type) 의 이름


## List Ops
<h3 id="artifactType-artifactVersions"><code>artifactType-artifactVersions</code></h3>

[artifactType]( artifact-type) 의 모든 [artifacts]( artifact) 의 [artifactVersions]( artifact-version) 를 반환합니다.

| Argument |  |
| :--- | :--- |
| `artifactType` | [artifactType]( artifact-type) |

#### Return Value
[artifactType]( artifact-type) 의 모든 [artifacts]( artifact) 의 [artifactVersions]( artifact-version)

<h3 id="artifactType-artifacts"><code>artifactType-artifacts</code></h3>

[artifactType](artifact-type) 의 [artifacts]( artifact) 를 반환합니다.

| Argument |  |
| :--- | :--- |
| `artifactType` | [artifactType]( artifact-type) |

#### Return Value
[artifactType]( artifact-type) 의 [artifacts]( artifact)

<h3 id="artifactType-name"><code>artifactType-name</code></h3>

[artifactType]( artifact-type) 의 이름을 반환합니다.

| Argument |  |
| :--- | :--- |
| `artifactType` | [artifactType]( artifact-type) |

#### Return Value
[artifactType]( artifact-type) 의 이름
